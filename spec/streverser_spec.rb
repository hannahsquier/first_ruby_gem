require 'spec_helper'

describe Streverser do
  it 'has a version number' do
    expect(Streverser::VERSION).not_to be nil
  end

  it 'does something useful' do
    expect(false).to eq(true)
  end
end
